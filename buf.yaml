version: v1beta1

name: buf.build/einride/protobuf-avro-go
deps:
  - buf.build/beta/googleapis

build:
  roots:
    - internal/examples/proto/src

lint:
  use:
    - DEFAULT
  except:
    - ENUM_VALUE_PREFIX
